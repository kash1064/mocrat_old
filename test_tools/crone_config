SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin:

# 毎日 07:15 に朝活通知を送信
15 7 * * * root `cd /home/ubuntu/mocrat && make asakatsu_scheduler > /dev/null 2>&1`

# 毎日 08:30 に朝活通知を送信
30 8 * * * root `curl -X GET http://localhost:8000/mocrat_utils/api/v1/asakatsu_closing/ > /dev/null 2>&1`

# 毎日 22:30 に振り返り通知
* * * * * sh -c "cd /home/ubuntu/mocrat && make furikaeri_reminder" > /var/log/mocrat_cron.log 2>&1

# 
* * * * * sh -c "cd /home/ubuntu/mocrat && make discord_heartbeat" > /var/log/mocrat_cron.log 2>&1

# 毎日 18:30 から 19:30 まで、20 分ごとに最大 20 個ずつのいいね
30 18 * * * root sh -c "cd ~/mocrat && make call_auto_fav_by_query twitter_auth_userid= auto_fav_word= auto_fav_count=20" > /dev/null 2>&1